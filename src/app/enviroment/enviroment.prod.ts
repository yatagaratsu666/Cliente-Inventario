// entorno producción

export const environment = {
  /** Indica si la aplicación se ejecuta en modo producción. */
  production: true,

  /** URL base de la API utilizada en producción (pero aun no monto eso a la maquina asiq zzz) */
//   apiUrl: '...'
};